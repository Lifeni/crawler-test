<!DOCTYPE html>
<html lang="zh-cn">
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.0.0-alpha1/css/bootstrap.min.css"
          rel="stylesheet">
    <title>Faker Test #<%= page %></title>
    <style>
        body {
            font-family: "Open Sans", "Helvetica Neue", Helvetica, Arial, sans-serif;
        }
    </style>
</head>
<body class="py-5">
<main>
    <section class="py-5 text-center container">
        <div class="row py-5">
            <div class="col-lg-6 col-md-8 mx-auto">
                <h1 class="py-2">Faker Test #<%= page %></h1>
                <p class="lead text-muted">用于 Python 爬虫的测试网页</p>
            </div>
        </div>
    </section>
    <div class="album py-5">
        <div class="container">
            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
                <% for(let i = 0; i < item; i++) { %>
                    <% if (images[i]) { %>
                        <div class="col">
                            <div class="card">
                                <img class="card-img-top img-fluid" src="<%= images[i] %>" alt="<%= fakers[i].name %>"
                                     title="<%= fakers[i].imageName %>" data-id="<%= fakers[i].imageId %>">
                                <div class="card-body">
                                    <small class="like text-muted mr-1"
                                           title="Like Count">
                                        Like <%= fakers[i].imageLikeCount %>
                                    </small>
                                    <small class="download text-muted"
                                           title="Download Count">
                                        Download <%= fakers[i].imageDownloadCount %>
                                    </small>
                                    <h5 class="card-title pt-1"><%= fakers[i].imageName %></h5>
                                    <p class="card-text"><%= fakers[i].imageDescription %></p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <span class="text-muted">
                                            <small class="author text-muted"
                                                   data-id="<%= fakers[i].authorId %>"
                                                   data-email="<%= fakers[i].authorEmail %>"
                                                   title="<%= fakers[i].authorEmail %>">
                                                <%= fakers[i].authorName %>
                                            </small>
                                            @
                                            <small class="follower text-muted"
                                                   title="Followers Count">
                                                <%= fakers[i].authorFollowerCount %>
                                            </small>
                                        </span>

                                        <small class="date text-muted"
                                               data-date="<%= fakers[i].imageDate %>"
                                               title="<%= fakers[i].imageDate %>"><%= fakers[i].imageDateFromNow %></small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</main>
<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center pt-4">
        <li class="page-item <%= 1 === page ? 'disabled' : '' %>">
            <a class="page-link" href="?page=<%= 1 === page ? 1 : page - 1 %>">Previous</a>
        </li>
        <% for(let i = 0; i < num; i++) { %>
            <li class="page-item <%= (i + 1) === page ? 'active' : '' %>">
                <a class="page-link" href="?page=<%= i + 1 %>"><%= i + 1 %></a>
            </li>
        <% } %>
        <li class="page-item <%= num === page ? 'disabled' : '' %>">
            <a class="page-link" href="?page=<%= num === page ? num : page + 1 %>">Next</a>
        </li>
    </ul>
</nav>
<script src="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.0.0-alpha1/js/bootstrap.bundle.min.js"></script>
</body>
</html>
